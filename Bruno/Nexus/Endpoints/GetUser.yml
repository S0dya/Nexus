info:
  name: GetUser
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/profile/user/"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
